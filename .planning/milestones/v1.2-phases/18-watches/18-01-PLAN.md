---
phase: 18-watches
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/watchManager.js
  - package.json
autonomous: true
requirements:
  - WATCH-01
  - WATCH-02
  - WATCH-03
  - WATCH-04
  - WATCH-06

must_haves:
  truths:
    - "WatchManager can store a watch definition (site, account, url, selector, schedule, webhookUrl)"
    - "WatchManager schedules runs using a cron expression and fires them automatically"
    - "Each run logs into the site with Puppeteer, navigates to the URL, captures text content and a PNG screenshot"
    - "WatchManager compares current snapshot to previous and detects content and/or visual changes"
    - "When a change is detected, WatchManager calls deliverWebhook with the change payload"
    - "Each run result is appended to per-watch history and persisted to disk"
  artifacts:
    - path: "src/agent/watchManager.js"
      provides: "WatchManager class with schedule, run, diff, webhook, and persistence logic"
      exports: ["WatchManager"]
    - path: "package.json"
      provides: "node-cron dependency"
      contains: "node-cron"
  key_links:
    - from: "src/agent/watchManager.js"
      to: "src/utils/browser.js"
      via: "createBrowser() for Puppeteer sessions"
      pattern: "require.*browser"
    - from: "src/agent/watchManager.js"
      to: "src/utils/webhook.js"
      via: "deliverWebhook() on change detection"
      pattern: "require.*webhook"
    - from: "src/agent/watchManager.js"
      to: "node-cron"
      via: "cron.schedule() for timed execution"
      pattern: "require.*node-cron"
---

<objective>
Build the WatchManager agent — the core engine for Phase 18 Watches.

Purpose: Provide a persistent, cron-scheduled page monitor that logs in to authenticated sites, captures content and screenshots, diffs against previous snapshots, fires webhooks on change, and stores run history to disk.

Output: src/agent/watchManager.js (WatchManager class) and node-cron added to package.json.
</objective>

<execution_context>
@/Users/benlamm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benlamm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From src/utils/browser.js:
```javascript
async function createBrowser(options = {})
// options: { headless=true, extraArgs=[], viewport={width:1920,height:1080} }
// returns: { browser, page }
```

From src/utils/webhook.js:
```javascript
async function deliverWebhook(webhookUrl, payload, options = {})
// options: { secret, operationType, operationId }
// returns: { status, url, attempts, lastAttemptAt, statusCode, error }
```

From src/utils/config.js:
```javascript
function loadCredentials()
// returns: { sites: { [siteName]: { baseUrl, accounts: { [accountType]: accountConfig } } } }
// accountConfig: { loginUrl, username, password, skipLogin? }
```

From src/agent/actions.js login pattern:
- goto baseUrl + accountConfig.loginUrl
- wait 3s for page load
- fill input[type="email"] or input[name="username"] with username
- fill input[type="password"] with password
- click [type="submit"] or button:last-of-type
- wait 2s
- if still on login URL path: login failed
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install node-cron</name>
  <files>package.json</files>
  <action>
Run: npm install node-cron

in /Users/benlamm/Workspace/khai-share.

node-cron provides cron expression scheduling (e.g. "0 * * * *" = hourly) using Node.js timers. No binary dependency. Verify package.json contains "node-cron" in dependencies.
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/khai-share && node -e "require('node-cron'); console.log('ok')"</automated>
  </verify>
  <done>node-cron importable; package.json has the dependency entry</done>
</task>

<task type="auto">
  <name>Task 2: Implement WatchManager agent</name>
  <files>src/agent/watchManager.js</files>
  <action>
Create /Users/benlamm/Workspace/khai-share/src/agent/watchManager.js implementing the WatchManager class.

PERSISTENCE PATH: config/watches.json (relative to project root, i.e. path.join(__dirname, '../../config/watches.json'))
SCREENSHOT DIR: screenshots/watches/{watchId}/{runId}.png

WATCH DATA MODEL:
```
{
  id: string (uuid),
  site: string,         // key in credentials.json sites
  account: string,      // key in siteConfig.accounts
  url: string,          // full URL or path (relative paths joined with baseUrl)
  selector: string|null, // CSS selector; null means full body text
  schedule: string,     // cron expression e.g. "*/5 * * * *"
  webhookUrl: string|null,
  enabled: boolean,     // default true
  createdAt: ISO string,
  updatedAt: ISO string,
}
```

RUN RECORD DATA MODEL:
```
{
  runId: string (uuid),
  watchId: string,
  timestamp: ISO string,
  status: "completed" | "changed" | "error" | "login-failed",
  contentHash: string,       // sha256 hex of extracted text
  screenshotPath: string,    // relative path from project root
  contentText: string,       // extracted text truncated to 50KB
  changed: boolean,
  diff: {
    contentChanged: boolean,
    visualChanged: boolean,
    pixelDiffPercent: number|null,
    contentBefore: string|null,  // first 500 chars of previous
    contentAfter: string|null,   // first 500 chars of current
  } | null,
  webhook: object|null,      // deliverWebhook() result
  error: string|null,
}
```

METHODS:

constructor():
- this.watchesPath = path.join(__dirname, '../../config/watches.json')
- this.screenshotsBase = path.join(__dirname, '../../screenshots/watches')
- this.watches = new Map()
- this.history = new Map()
- this.cronJobs = new Map()
- this._load()

addWatch(config):
- Required: site, account, url, schedule
- If any missing: throw Error('site, account, url, and schedule are required')
- If !cron.validate(config.schedule): throw Error('Invalid cron expression: ' + config.schedule)
- Build watch object with id=uuidv4(), enabled=true, createdAt/updatedAt=now
- this.watches.set(id, watch)
- this.history.set(id, [])
- if watch.enabled: this._scheduleWatch(watch)
- this._save()
- return watch

updateWatch(id, updates):
- If !this.watches.has(id): return null
- Only allow updating: url, selector, schedule, webhookUrl, enabled
- If updates.schedule provided: validate with cron.validate; throw on invalid
- Merge into existing, set updatedAt
- Stop old cron job: const existing = this.cronJobs.get(id); if (existing) { existing.stop(); this.cronJobs.delete(id); }
- If updated.enabled: this._scheduleWatch(updated)
- this._save()
- return updated watch

removeWatch(id):
- If !this.watches.has(id): return false
- Stop cron job if running
- this.watches.delete(id)
- this.history.delete(id)
- Delete screenshot dir screenshots/watches/{id} with fs.rmSync({recursive:true, force:true})
- this._save()
- return true

getWatch(id): return this.watches.get(id) || null

listWatches():
- Return [...this.watches.values()].map(w => ({ ...w, running: this.cronJobs.has(w.id) }))

getHistory(id, limit = 20):
- return (this.history.get(id) || []).slice(-limit)

startAll():
- For each watch in this.watches: if watch.enabled, this._scheduleWatch(watch)
- Log count

stopAll():
- For each task in this.cronJobs: task.stop()
- this.cronJobs.clear()

_scheduleWatch(watch):
- Stop existing job: const existing = this.cronJobs.get(watch.id); if (existing) { existing.stop(); this.cronJobs.delete(watch.id); }
- const task = cron.schedule(watch.schedule, () => this._runWatch(watch.id), { timezone: 'UTC' })
- this.cronJobs.set(watch.id, task)
- console.log('[WatchManager] Scheduled watch', watch.id, 'on', watch.schedule)

async _runWatch(watchId):
- const watch = this.watches.get(watchId); if (!watch) return;
- const runId = uuidv4()
- const timestamp = new Date().toISOString()
- const screenshotDir = path.join(this.screenshotsBase, watchId)
- const screenshotPath = path.join(screenshotDir, runId + '.png')
- const screenshotRelative = path.relative(path.join(__dirname, '../..'), screenshotPath)
- fs.mkdirSync(screenshotDir, { recursive: true })

- Load credentials; get siteConfig and accountConfig:
  const credentials = loadCredentials()
  const siteConfig = credentials.sites[watch.site]
  if (!siteConfig) { record error 'Site not found'; append; save; return }
  const accountConfig = siteConfig.accounts[watch.account]
  if (!accountConfig) { record error 'Account not found'; append; save; return }

- const fullUrl = watch.url.startsWith('http') ? watch.url : siteConfig.baseUrl + watch.url

- try {
    const { browser, page } = await createBrowser()
    let loggedIn = true

    if (!accountConfig.skipLogin) {
      await page.goto(siteConfig.baseUrl + accountConfig.loginUrl, { waitUntil: 'networkidle2', timeout: 30000 })
      await page.waitForTimeout(3000)
      // Fill login form
      try {
        const usernameSelector = 'input[type="email"], input[name="username"], input[name="email"], input[name="user_login"]'
        await page.$eval(usernameSelector, (el, val) => { el.value = val }, accountConfig.username)
        await page.$eval('input[type="password"]', (el, val) => { el.value = val }, accountConfig.password)
        await Promise.all([
          page.waitForNavigation({ waitUntil: 'networkidle2', timeout: 15000 }).catch(() => {}),
          page.$eval('form', f => f.submit()).catch(() =>
            page.$eval('[type="submit"], button[type="submit"]', b => b.click())
          )
        ])
        await page.waitForTimeout(2000)
        // Detect login failure: still on login URL path
        const currentUrl = page.url()
        if (currentUrl.includes(accountConfig.loginUrl)) {
          loggedIn = false
        }
      } catch (loginErr) {
        loggedIn = false
        console.error('[WatchManager] Login error:', loginErr.message)
      }
    }

    if (!loggedIn) {
      await browser.close()
      // Append login-failed record
      const record = { runId, watchId, timestamp, status: 'login-failed', contentHash: '', screenshotPath: screenshotRelative, contentText: '', changed: false, diff: null, webhook: null, error: 'Login failed' }
      this._appendRecord(watchId, record)
      this._save()
      console.log('[WatchManager] Watch', watchId, 'run', runId, ': login-failed')
      return
    }

    await page.goto(fullUrl, { waitUntil: 'networkidle2', timeout: 30000 })
    await page.waitForTimeout(1000)

    // Extract content
    let contentText = ''
    if (watch.selector) {
      try { contentText = await page.$eval(watch.selector, el => el.innerText) } catch (_) {}
    } else {
      contentText = await page.$eval('body', el => el.innerText)
    }
    contentText = contentText.slice(0, 50 * 1024)

    await page.screenshot({ path: screenshotPath, fullPage: true })
    await browser.close()

    // Hash content
    const contentHash = crypto.createHash('sha256').update(contentText).digest('hex')

    // Get previous run
    const prevRuns = this.history.get(watchId) || []
    const prevRun = prevRuns.length > 0 ? prevRuns[prevRuns.length - 1] : null

    // Compute diff
    let diff = null
    let changed = false
    if (prevRun) {
      const contentChanged = contentHash !== prevRun.contentHash
      let visualChanged = false
      let pixelDiffPercent = null

      // Visual diff using pixelmatch
      const prevScreenshotAbs = path.join(__dirname, '../..', prevRun.screenshotPath)
      if (fs.existsSync(prevScreenshotAbs) && fs.existsSync(screenshotPath)) {
        try {
          const prevPng = PNG.sync.read(fs.readFileSync(prevScreenshotAbs))
          const currPng = PNG.sync.read(fs.readFileSync(screenshotPath))
          if (prevPng.width === currPng.width && prevPng.height === currPng.height) {
            const { width, height } = currPng
            const diffPng = new PNG({ width, height })
            const numDiffPixels = pixelmatch(prevPng.data, currPng.data, diffPng.data, width, height, { threshold: 0.1 })
            pixelDiffPercent = numDiffPixels / (width * height)
            visualChanged = pixelDiffPercent > 0.01
          }
        } catch (diffErr) {
          console.error('[WatchManager] Visual diff error:', diffErr.message)
        }
      }

      changed = contentChanged || visualChanged
      diff = {
        contentChanged,
        visualChanged,
        pixelDiffPercent,
        contentBefore: prevRun.contentText ? prevRun.contentText.slice(0, 500) : null,
        contentAfter: contentText.slice(0, 500),
      }
    }

    const status = changed ? 'changed' : 'completed'

    // Fire webhook on change
    let webhookResult = null
    if (changed && watch.webhookUrl) {
      const payload = { watchId, runId, watch: { site: watch.site, account: watch.account, url: watch.url, selector: watch.selector }, diff, contentText, screenshotPath: screenshotRelative, timestamp }
      webhookResult = await deliverWebhook(watch.webhookUrl, payload, { operationType: 'watch', operationId: runId })
    }

    const record = { runId, watchId, timestamp, status, contentHash, screenshotPath: screenshotRelative, contentText, changed, diff, webhook: webhookResult, error: null }
    this._appendRecord(watchId, record)
    this._save()
    console.log('[WatchManager] Watch', watchId, 'run', runId, ': status=' + status)

  } catch (err) {
    console.error('[WatchManager] Run error for watch', watchId, ':', err.message)
    const record = { runId, watchId, timestamp, status: 'error', contentHash: '', screenshotPath: screenshotRelative, contentText: '', changed: false, diff: null, webhook: null, error: err.message }
    this._appendRecord(watchId, record)
    this._save()
  }

_appendRecord(watchId, record):
- if (!this.history.has(watchId)) this.history.set(watchId, [])
- const hist = this.history.get(watchId)
- hist.push(record)
- if (hist.length > 100) this.history.set(watchId, hist.slice(-100))

_save():
- try {
    const data = { watches: [...this.watches.values()], history: Object.fromEntries(this.history) }
    const tmp = this.watchesPath + '.tmp'
    fs.mkdirSync(path.dirname(this.watchesPath), { recursive: true })
    fs.writeFileSync(tmp, JSON.stringify(data, null, 2))
    fs.renameSync(tmp, this.watchesPath)
  } catch (err) {
    console.error('[WatchManager] Save failed:', err.message)
  }

_load():
- if (!fs.existsSync(this.watchesPath)) return
- try {
    const data = JSON.parse(fs.readFileSync(this.watchesPath, 'utf-8'))
    if (data.watches) for (const w of data.watches) this.watches.set(w.id, w)
    if (data.history) for (const [id, runs] of Object.entries(data.history)) this.history.set(id, runs)
    console.log('[WatchManager] Loaded', this.watches.size, 'watch(es) from disk')
  } catch (err) {
    console.error('[WatchManager] Load failed:', err.message)
  }

REQUIRED IMPORTS:
```javascript
'use strict';
const fs = require('fs');
const path = require('path');
const crypto = require('crypto');
const cron = require('node-cron');
const { v4: uuidv4 } = require('uuid');
const { PNG } = require('pngjs');
const pixelmatch = require('pixelmatch');
const { createBrowser } = require('../utils/browser');
const { loadCredentials } = require('../utils/config');
const { deliverWebhook } = require('../utils/webhook');
```
  </action>
  <verify>
    <automated>cd /Users/benlamm/Workspace/khai-share && node -e "const WatchManager = require('./src/agent/watchManager'); const wm = new WatchManager(); const w = wm.addWatch({ site: 'test', account: 'admin', url: 'http://example.com', schedule: '0 * * * *' }); console.assert(w.id, 'watch has id'); console.assert(wm.listWatches().length === 1, 'listed'); wm.removeWatch(w.id); console.assert(wm.listWatches().length === 0, 'removed'); wm.stopAll(); console.log('PASS')"</automated>
  </verify>
  <done>
    - WatchManager instantiates without error
    - addWatch validates cron expression and stores watch
    - removeWatch deletes watch
    - listWatches returns watches with running field
    - getHistory returns empty array for new watch
    - _runWatch logic is implemented (integration tested via routes in plan 02)
  </done>
</task>

</tasks>

<verification>
Run: cd /Users/benlamm/Workspace/khai-share && node -e "const WatchManager = require('./src/agent/watchManager'); console.log('WatchManager loads OK')"

Confirm no import errors. Confirm package.json includes node-cron.
</verification>

<success_criteria>
- node-cron installed and importable
- WatchManager class exported from src/agent/watchManager.js
- addWatch, updateWatch, removeWatch, listWatches, getHistory, startAll, stopAll methods present
- Cron validation works (valid expression accepted, invalid rejected)
- Persistence to config/watches.json confirmed
- No unhandled exceptions on basic operations
</success_criteria>

<output>
After completion, create .planning/phases/18-watches/18-01-SUMMARY.md
</output>
