---
phase: 17-webhooks
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - khai_mcp/server.py
  - CLAUDE.md
  - README.md
autonomous: true
requirements: [HOOK-01, HOOK-02, HOOK-04]

must_haves:
  truths:
    - "MCP tools khai_start_test, khai_execute_actions, khai_run_audit, and khai_check_links accept webhook_url parameter"
    - "MCP tools pass webhookUrl through to the REST API"
    - "CLAUDE.md documents webhook parameters and KHAI_WEBHOOK_SECRET env var"
    - "README.md documents webhook feature in features list"
    - "MCP instructions string mentions webhook capability"
  artifacts:
    - path: "khai_mcp/server.py"
      provides: "webhook_url parameter on 4 MCP tools + updated instructions"
      contains: "webhook_url"
    - path: "CLAUDE.md"
      provides: "Webhook documentation for users"
      contains: "webhookUrl"
    - path: "README.md"
      provides: "Webhook feature in README"
      contains: "webhook"
  key_links:
    - from: "khai_mcp/server.py"
      to: "src/routes/api.js"
      via: "POST /api/test/start with webhookUrl in body"
      pattern: "webhookUrl.*webhook_url"
    - from: "khai_mcp/server.py"
      to: "src/routes/actions.js"
      via: "POST /api/actions/execute with webhookUrl in body"
      pattern: "webhookUrl.*webhook_url"
---

<objective>
Add webhook_url parameter to MCP tools and update all documentation (CLAUDE.md, README.md, MCP instructions) to reflect the webhook feature.

Purpose: Makes webhooks accessible from Claude Code via MCP tools (not just raw REST API), and ensures docs stay in sync per project CLAUDE.md Doc Update Rule.
Output: MCP tools accept webhook_url, docs are updated.
</objective>

<execution_context>
@/Users/benlamm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benlamm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-webhooks/17-01-SUMMARY.md

@khai_mcp/server.py
@khai_mcp/client.py
@CLAUDE.md
@README.md
</context>

<interfaces>
<!-- From Plan 01: webhook utility contract -->
From src/utils/webhook.js:
```javascript
// deliverWebhook(webhookUrl, payload, options) => Promise<WebhookStatus>
// WebhookStatus: { status: 'delivered'|'failed', url, attempts, lastAttemptAt, statusCode, error }
```

<!-- REST API changes from Plan 01 -->
All start endpoints now accept optional `webhookUrl` in request body.
All status endpoints now include `webhook: { status, url, attempts, ... } | null` in response.
Env var: `KHAI_WEBHOOK_SECRET` for HMAC-SHA256 signing.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add webhook_url parameter to MCP tools</name>
  <files>khai_mcp/server.py</files>
  <action>
Update `khai_mcp/server.py` to add `webhook_url` parameter to the four async operation tools and update the MCP instructions string.

**Tool parameter additions:**

1. `khai_start_test` — add parameter `webhook_url: str | None = None`:
   - Docstring: `webhook_url: URL to POST results to when test completes (optional). Set KHAI_WEBHOOK_SECRET env var for HMAC-SHA256 signing.`
   - In payload construction, add: `if webhook_url: payload["webhookUrl"] = webhook_url`

2. `khai_execute_actions` — add parameter `webhook_url: str | None = None`:
   - Same docstring pattern
   - Add to the POST body: `if webhook_url: payload["webhookUrl"] = webhook_url`
   - Note: payload dict is constructed inline in the `client.post` call — extract to a variable first, then conditionally add webhookUrl

3. `khai_run_audit` — add parameter `webhook_url: str | None = None`:
   - Same pattern, add to payload

4. `khai_check_links` — add parameter `webhook_url: str | None = None`:
   - Same pattern, add to the POST body sent to `/api/advanced/links/check`

**Instructions string update:**

Add to the MCP instructions string (the `instructions=` parameter in the FastMCP constructor), after the "All operations are async" line:

```
Webhooks: Any start operation accepts an optional webhook_url parameter. When provided,
Khai will POST the full results to that URL when the operation completes. Payloads are
signed with HMAC-SHA256 if KHAI_WEBHOOK_SECRET is set. Delivery retries up to 3 times
with exponential backoff. Check the webhook field in results for delivery status.
```

Also update the tool list in instructions to mention webhook support:
```
- khai_start_test: Start an authenticated crawl test on a site (supports webhook_url)
- khai_execute_actions: Run a sequence of browser actions (supports webhook_url)
- khai_run_audit: Start a security/configuration audit (supports webhook_url)
- khai_check_links: Check a site for broken links (supports webhook_url)
```
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
with open('khai_mcp/server.py') as f:
    tree = ast.parse(f.read())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
webhook_funcs = ['khai_start_test', 'khai_execute_actions', 'khai_run_audit', 'khai_check_links']
for fn_name in webhook_funcs:
    found = False
    for node in ast.walk(tree):
        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)) and node.name == fn_name:
            args = [a.arg for a in node.args.args]
            if 'webhook_url' in args:
                found = True
    if not found:
        print(f'FAIL: {fn_name} missing webhook_url param')
        sys.exit(1)
print('OK: all 4 tools have webhook_url parameter')
"</automated>
  </verify>
  <done>
    - All 4 MCP tools (khai_start_test, khai_execute_actions, khai_run_audit, khai_check_links) accept webhook_url parameter
    - webhook_url is passed as webhookUrl in the POST body to the REST API
    - MCP instructions string documents webhook capability
    - Python syntax is valid
  </done>
</task>

<task type="auto">
  <name>Task 2: Update documentation (CLAUDE.md and README.md)</name>
  <files>CLAUDE.md, README.md</files>
  <action>
Update both documentation files per the project's Doc Update Rule (CLAUDE.md, README.md, and MCP instructions — instructions handled in Task 1).

**CLAUDE.md updates:**

1. In the **MCP Tools** table, update descriptions for the 4 tools that now support webhooks:
   - `khai_start_test` → "Start an authenticated crawl test (supports webhookUrl)"
   - `khai_execute_actions` → "Run browser action sequences (supports webhookUrl)"
   - `khai_run_audit` → "Start a security/configuration audit (supports webhookUrl)"
   - `khai_check_links` → "Check a site for broken links (supports webhookUrl)"

2. Add a new section **## Webhooks** after the "Async Polling Pattern" section:
   ```markdown
   ## Webhooks

   Any async start operation accepts an optional `webhookUrl` parameter. When provided, Khai POSTs the full operation results to that URL on completion.

   - **Signing**: Set `KHAI_WEBHOOK_SECRET` env var. Khai computes HMAC-SHA256 of the JSON body and sends it as `X-Khai-Signature: sha256=<hex>`.
   - **Retry**: Up to 3 attempts with exponential backoff (1s, 4s, 16s). No retry on 4xx.
   - **Status**: Results include a `webhook` field: `{status: "delivered"|"failed", attempts, ...}`.
   - **Headers**: `X-Khai-Event: operation.completed`, `X-Khai-Operation: test|audit|action|link-check`, `X-Khai-Operation-Id: <id>`.
   ```

3. Add webhook example to the REST API sections. Under **Crawl Testing**, update the "Start a crawl test" curl example to show optional webhookUrl:
   ```bash
   # Start a crawl test with webhook notification
   curl -X POST http://localhost:3001/api/test/start \
     -H "Content-Type: application/json" \
     -d '{"site": "yoursite.com", "account": "admin", "webhookUrl": "https://example.com/webhook"}'
   ```

4. In the **Configuration Files** table, add:
   ```
   | `KHAI_WEBHOOK_SECRET` env var | Shared secret for HMAC-SHA256 webhook signatures |
   ```

5. In the "When to Suggest Khai" list, add: "Webhook notifications on operation completion"

**README.md updates:**

Read the current README.md first. Add webhook documentation:

1. If there is a features list, add "Webhook notifications with HMAC-SHA256 signing"
2. Add a Webhooks section describing the feature briefly
3. Mention the `KHAI_WEBHOOK_SECRET` env var for signing
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const claude = fs.readFileSync('CLAUDE.md', 'utf8');
const readme = fs.readFileSync('README.md', 'utf8');
const checks = [
  ['CLAUDE.md has webhookUrl', claude.includes('webhookUrl')],
  ['CLAUDE.md has KHAI_WEBHOOK_SECRET', claude.includes('KHAI_WEBHOOK_SECRET')],
  ['CLAUDE.md has X-Khai-Signature', claude.includes('X-Khai-Signature')],
  ['README.md has webhook', readme.toLowerCase().includes('webhook')],
];
let ok = true;
for (const [name, pass] of checks) {
  if (!pass) { console.log('FAIL:', name); ok = false; }
}
if (ok) console.log('OK: all doc checks pass');
else process.exit(1);
"</automated>
  </verify>
  <done>
    - CLAUDE.md documents webhookUrl parameter, KHAI_WEBHOOK_SECRET, X-Khai-Signature header, retry behavior
    - CLAUDE.md MCP tools table mentions webhook support
    - CLAUDE.md "When to Suggest Khai" includes webhook use case
    - README.md mentions webhook feature
    - All three doc targets updated per Doc Update Rule (CLAUDE.md, README.md, MCP instructions in Task 1)
  </done>
</task>

</tasks>

<verification>
1. `python3 -c "import khai_mcp.server"` succeeds (no import errors)
2. All 4 MCP tools accept webhook_url parameter
3. CLAUDE.md, README.md both document webhook feature
4. MCP instructions string mentions webhooks
</verification>

<success_criteria>
- Claude Code users can pass webhook_url to any of the 4 start MCP tools
- All three documentation targets (CLAUDE.md, README.md, MCP instructions) are updated
- Documentation explains signing, retry, and status tracking
</success_criteria>

<output>
After completion, create `.planning/phases/17-webhooks/17-02-SUMMARY.md`
</output>
